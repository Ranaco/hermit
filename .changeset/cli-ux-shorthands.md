---
"@hermit-kms/cli": minor
---

Add top-level shorthand commands: `hermit ls [path]`, `hermit get <path/name>`, `hermit set <path/name> [value]`, `hermit rm <path/name>`, `hermit tree [path]`, `hermit login`, `hermit logout`. Positional path argument for `hermit run` (`hermit run prod/api -- npm run dev`).
