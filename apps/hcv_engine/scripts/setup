#!/usr/bin/env sh
set -eu

echo "Deprecated: automatic Vault setup on container startup has been removed." >&2
echo "Use /vault/scripts/bootstrap-vault.sh and /vault/scripts/provision-vault.sh explicitly." >&2
exit 1
